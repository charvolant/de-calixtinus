<div .container-fluid>
  <div .row .m-5>
    <div .col>
      <p>
        ^{embedded}
        _{top}
  <div .row .justify-content-center>
    <div .col-10>
      <form .container-fluid method=post action=@{PreferencesR} enctype="#{enctype}">
        ^{widget}
        <input type=hidden name="_step" value="#{show stepp}">
        <div .btn-toolbar .justify-content-between>
          <div .btn-group>
            <button type="submit" name="_next" value="#{show prevp}" .btn .btn-secondary>_{MsgBackLabel}
          <div .btn-group>
            <button type="submit" name="_next" value="#{show nextp}" .btn .btn-primary>_{MsgNextLabel}
  $maybe bot <- bottom
    <div .row .m-5>
      <div .col>
        <p>_{bot}
  ^{help}
